// const button = document.getElementById('bolt');
// const butt = document.getElementById('bash');
// const but = document.getElementById('bard');
const but = [2,3,4,5,6,7,8]
var x = []
for (const i of but) {
	if (i = but.length) {
		x = i	
	}else if(i = but,length){
		x = i
	}
}
console.log(x)

// button.addEventListener('click', ()=>take('name'));
// butt.addEventListener('click', ()=>take(''));
// but.addEventListener('click', ()=>take(x ? ()=>take('name') : ()=>take('')))


// function take(path){
// 	fetch('http://localhost:3000/'+path,{
// 		method: 'GET',
// 		mode: 'cors'
// 	} ).then(response => response.json())
// 	.then(response => console.log(response))
// 	.catch(err => console.error(err));

// }
